@model SilkFlo.Web.ViewModels.Dashboard.Component.PipelineBenefitsByStage

@Html.Raw(Model.DoughnutChart.SVG)

<div class="d-flex justify-content-between"
     style="width: 100%; margin-top: 0.5rem;">
    @foreach (var chartKey in Model.PieChartKeys)
    {
        @await Html.PartialAsync("../Shared/Dashboard/_PieChartkey", chartKey)
    }
</div>