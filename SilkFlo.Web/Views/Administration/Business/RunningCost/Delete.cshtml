@model SilkFlo.Web.Models.Business.RunningCost
<div class="container-fluid">

    <form asp-action="Delete"
          method="post">
        <div class="TitleBar">
            <h2>Delete Running Cost</h2>

            <h3>Are you sure you?</h3>

            <div class="grid_1fr_auto">
                <div class="toolbar">
                    <!-- Buttons -->
                    <button formaction="/Administration/Business/RunningCost/Delete"
                            formmethod="post"
                            type="submit"
                            class="btn btn-danger">Delete</button>
                    <button formaction="/Administration/Business/RunningCost/Cancel"
                            formmethod="post"
                            type="submit"
                            class="btn btn-secondary">Cancel</button>
                </div>

                <div class="toolbar">
                    <a href="/Administration/Business/RunningCost">Back to List</a>
                </div>
            </div>
        </div>
    @if (!ViewContext.ModelState.IsValid)
    {
        @Html.ValidationSummary(false,
                                "",
                                new { @class = "text-danger" })
    }

        @Html.HiddenFor(x => x.Id)
        @Html.HiddenFor(x => x.CreatedById)
        @Html.HiddenFor(x => x.CreatedDate)
        @Html.HiddenFor(x => x.UpdatedById)
        @Html.HiddenFor(x => x.UpdatedDate)

        @Html.HiddenFor(x => x.AutomationTypeId)
        @Html.HiddenFor(x => x.ClientId)
        @Html.HiddenFor(x => x.Cost)
        @Html.HiddenFor(x => x.FrequencyId)
        @Html.HiddenFor(x => x.IsLive)
        @Html.HiddenFor(x => x.LicenceType)
        @Html.HiddenFor(x => x.VenderId)
        <div class="grid_auto_1fr">

            <!-- Cost -->
            <div>@Html.DisplayNameFor(x => x.Cost)</div>
            <div>@Html.DisplayFor(x => x.Cost)</div>

            <!-- IsLive -->
            <div>@Html.DisplayNameFor(x => x.IsLive)</div>
            <div>@Html.DisplayFor(x => x.IsLive)</div>

            <!-- LicenceType -->
            <div>@Html.DisplayNameFor(x => x.LicenceType)</div>
            <div>@Html.DisplayFor(x => x.LicenceType)</div>

            <!-- AutomationTypeString -->
            <div>@Html.DisplayNameFor(x => x.AutomationTypeString)</div>
            <div>@Html.DisplayFor(x => x.AutomationTypeString)</div>

            <!-- ClientString -->
            <div>@Html.DisplayNameFor(x => x.ClientString)</div>
            <div>@Html.DisplayFor(x => x.ClientString)</div>

            <!-- FrequencyString -->
            <div>@Html.DisplayNameFor(x => x.FrequencyString)</div>
            <div>@Html.DisplayFor(x => x.FrequencyString)</div>

            <!-- VenderString -->
            <div>@Html.DisplayNameFor(x => x.VenderString)</div>
            <div>@Html.DisplayFor(x => x.VenderString)</div>

            <!-- CreatedBy -->
            <div>Created By</div>
            <div>@Html.DisplayFor(x => x.CreatedBy)</div>

            <!-- Created Date -->
            <div>@Html.DisplayNameFor(x => x.CreatedDate)</div>
            <div>@Html.DisplayFor(x => x.CreatedDateString)</div>
        </div>
    </form>
</div>