@model SilkFlo.Web.Models.Business.IdeaStageStatus
<div class="container-fluid">

    <form asp-action="Delete"
          method="post">
        <div class="TitleBar">
            <h2>Delete Status</h2>

            <h3>Are you sure you?</h3>

            <div class="grid_1fr_auto">
                <div class="toolbar">
                    <!-- Buttons -->
                    <button formaction="/Administration/Business/IdeaStageStatus/Delete"
                            formmethod="post"
                            type="submit"
                            class="btn btn-danger">Delete</button>
                    <button formaction="/Administration/Business/IdeaStageStatus/Cancel"
                            formmethod="post"
                            type="submit"
                            class="btn btn-secondary">Cancel</button>
                </div>

                <div class="toolbar">
                    <a href="/Administration/Business/IdeaStageStatus">Back to List</a>
                </div>
            </div>
        </div>
    @if (!ViewContext.ModelState.IsValid)
    {
        @Html.ValidationSummary(false,
                                "",
                                new { @class = "text-danger" })
    }

        @Html.HiddenFor(x => x.Id)
        @Html.HiddenFor(x => x.CreatedById)
        @Html.HiddenFor(x => x.CreatedDate)
        @Html.HiddenFor(x => x.UpdatedById)
        @Html.HiddenFor(x => x.UpdatedDate)

        @Html.HiddenFor(x => x.Date)
        @Html.HiddenFor(x => x.IdeaStageId)
        @Html.HiddenFor(x => x.StatusId)
        <div class="grid_auto_1fr">

            <!-- Date -->
            <div>@Html.DisplayNameFor(x => x.Date)</div>
            <div>@Html.DisplayFor(x => x.Date)</div>

            <!-- IdeaStageString -->
            <div>@Html.DisplayNameFor(x => x.IdeaStageString)</div>
            <div>@Html.DisplayFor(x => x.IdeaStageString)</div>

            <!-- StatusString -->
            <div>@Html.DisplayNameFor(x => x.StatusString)</div>
            <div>@Html.DisplayFor(x => x.StatusString)</div>

            <!-- CreatedBy -->
            <div>Created By</div>
            <div>@Html.DisplayFor(x => x.CreatedBy)</div>

            <!-- Created Date -->
            <div>@Html.DisplayNameFor(x => x.CreatedDate)</div>
            <div>@Html.DisplayFor(x => x.CreatedDateString)</div>
        </div>
    </form>
</div>