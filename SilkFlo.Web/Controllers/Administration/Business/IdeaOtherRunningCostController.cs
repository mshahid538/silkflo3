/*********************************************************
       Code Generated By  .  .  .  .  Delaney's ScriptBot
       WWW .  .  .  .  .  .  .  .  .  www.scriptbot.io
       Template Name.  .  .  .  .  .  Project Green 3.0
       Template Version.  .  .  .  .  20210407 014
       Author .  .  .  .  .  .  .  .  Delaney

                      ,        ,--,_
                       \ _ ___/ /\|
                       ( )__, )
                      |/_  '--,
                        \ `  / '

 *********************************************************/

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using System;
using System.Threading.Tasks;
using SilkFlo.Data.Core;

namespace SilkFlo.Web.Controllers.Administration.Business
{
    public partial class IdeaOtherRunningCostController : Abstract
    {
        public IdeaOtherRunningCostController(IUnitOfWork unitOfWork,
                                  Services.ViewToString viewToString,
                                  IAuthorizationService authorisation) : base(unitOfWork, viewToString, authorisation) { }


        [HttpGet("/Administration/Business/IdeaOtherRunningCost")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Index()
        {
            try
            {
                IEnumerable<Data.Core.Domain.Business.IdeaOtherRunningCost> cores;

                cores = (await _unitOfWork.BusinessIdeaOtherRunningCosts
                                         .GetAllAsync())
                                         .ToArray();

                await _unitOfWork.BusinessClients.GetClientForAsync(cores);
                await _unitOfWork.BusinessIdeas.GetIdeaForAsync(cores);
                await _unitOfWork.BusinessOtherRunningCosts.GetOtherRunningCostForAsync(cores);

                var models = new List<Models.Business.IdeaOtherRunningCost>();

                if(cores != null)
                {
                    foreach(var core in cores)
                    {
                        var model = new Models.Business.IdeaOtherRunningCost(core);
                        models.Add(model);
                    }
                }

                var summary = new Models.Summary<Models.Business.IdeaOtherRunningCost>
                {
                    Models = models,
                    Count = models.Count,
                };

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Index.cshtml", summary);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                var summary = new Models.Summary<Models.Business.IdeaOtherRunningCost>
                {
                    Models = null,
                    Count = 0,
                };

                ModelState.AddModelError(
                        "Error",
                        Models.Log.Message_DatabaseErrorFetchList);

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Index.cshtml", summary);
            }
        }
        [HttpGet("/Administration/Business/IdeaOtherRunningCost/Delete/{id}")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete (string id)
        {
            if (id == null)
                return Redirect("/Administration/business/ideaOtherRunningCost");

            var ideaOtherRunningCost = await _unitOfWork.BusinessIdeaOtherRunningCosts.GetAsync(id);

            if (ideaOtherRunningCost == null)
                return Redirect("/Administration/Business/IdeaOtherRunningCost");
            else
            {
                await _unitOfWork.BusinessClients.GetClientForAsync(ideaOtherRunningCost);
                await _unitOfWork.BusinessIdeas.GetIdeaForAsync(ideaOtherRunningCost);
                await _unitOfWork.BusinessOtherRunningCosts.GetOtherRunningCostForAsync(ideaOtherRunningCost);

                if (ideaOtherRunningCost.CreatedById != null)
                    ideaOtherRunningCost.CreatedBy = await _unitOfWork.Users.GetAsync(ideaOtherRunningCost.CreatedById);

                if (ideaOtherRunningCost.UpdatedById != null)
                    ideaOtherRunningCost.UpdatedBy = await _unitOfWork.Users.GetAsync(ideaOtherRunningCost.UpdatedById);

                var model = new Models.Business.IdeaOtherRunningCost(ideaOtherRunningCost);

                return View("/Views/Administration/Business/IdeaOtherRunningCost/delete.cshtml", model);
            }
        }
        [HttpPost("/Administration/Business/IdeaOtherRunningCost/Delete")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete(Models.Business.IdeaOtherRunningCost model)
        {
            if (model == null)
                return Redirect("/Administration/Business/IdeaOtherRunningCost");


            var userId = GetUserId();

            if (userId == null)
            {
                ModelState.AddModelError("Error",
                                         "UserId is missing.");

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Delete.cshtml", model);
            }


            if (model.Id == null)
            {
                var message = "[POST] The primary key ID is missing.";

                ModelState.AddModelError("Error",
                                         message);

                _unitOfWork.Log(message,
                                Severity.Warning);

                await _unitOfWork.CompleteAsync();

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Delete.cshtml", model);
            }


            try
            {
                await _unitOfWork.BusinessIdeaOtherRunningCosts
                                 .RemoveAsync(model.Id);

                await _unitOfWork.CompleteAsync();

                return Redirect(
                    "/Administration/Business/IdeaOtherRunningCost");
            }
            catch (ChildDependencyException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Business/IdeaOtherRunningCost/Delete.cshtml", model);
            }
            catch (NotFoundException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Business/IdeaOtherRunningCost/Delete.cshtml", model);
            }
            catch (Exception ex)
            {
                ModelState.AddModelError("Error",
                                         "Cannot delete the IdeaOtherRunningCost.");

                _unitOfWork.Log(ex);
                await _unitOfWork.CompleteAsync();
                return View("/Views/Administration/Business/IdeaOtherRunningCost/Delete.cshtml", model);
            }
        }

        [HttpGet("/Administration/Business/IdeaOtherRunningCost/Edit/{id}")]
        [HttpGet("/Administration/Business/IdeaOtherRunningCost/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(string id)
        {
            Data.Core.Domain.Business.IdeaOtherRunningCost ideaOtherRunningCost;

            if (string.IsNullOrWhiteSpace(id))
            {
                ideaOtherRunningCost = new Data.Core.Domain.Business.IdeaOtherRunningCost
                {
                    Id = Guid.NewGuid().ToString(),
                };
            }
            else
            {
                ideaOtherRunningCost = await _unitOfWork.BusinessIdeaOtherRunningCosts.GetAsync(id);

                if (ideaOtherRunningCost == null)
                    return Redirect(
                        "/Administration/Business/IdeaOtherRunningCost");
                await _unitOfWork.BusinessClients.GetClientForAsync(ideaOtherRunningCost);
                await _unitOfWork.BusinessIdeas.GetIdeaForAsync(ideaOtherRunningCost);
                await _unitOfWork.BusinessOtherRunningCosts.GetOtherRunningCostForAsync(ideaOtherRunningCost);

            }

            var model = new Models.Business.IdeaOtherRunningCost(ideaOtherRunningCost);
            await model.GetCreatedAndUpdated(_unitOfWork);

            var coreClients = await _unitOfWork.BusinessClients.GetAllAsync();
            model.Clients.AddRange(Models.Business.Client.Create(coreClients));

            return View("/Views/Administration/Business/IdeaOtherRunningCost/Edit.cshtml", model);
        }


        [HttpPost("/Administration/Business/IdeaOtherRunningCost/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(Models.Business.IdeaOtherRunningCost model)
        {
            try
            {
                // Get the userId
                var userId = GetUserId();


                // Is the userId present.
                // If not return to view.                
                if (userId == null)
                {
                    ModelState.AddModelError(
                            "Error",
                            "userId is missing.");

                    return View("/Views/Administration/Business/IdeaOtherRunningCost/Edit.cshtml", model);
                }


                // Is the model valid?
                // If not return to view.
                bool isValid = Validate(_unitOfWork, model);

                var feedback = new ViewModels.Feedback();
                feedback = await model.CheckUniqueAsync(_unitOfWork, feedback);
                if (!feedback.IsValid)
                {
                    isValid = false;
                    foreach (var feedbackElement in feedback.Elements)
                        model.Errors.Add(feedbackElement.Value);
                }

                if (!isValid)
                {
                    // Get parent: Business.Client -> Client
                    await _unitOfWork.BusinessClients.GetClientForAsync(model.GetCore());
                    model.Client =
                        model.GetCore().Client == null ?
                            null :
                            new Models.Business.Client(model.GetCore().Client);

                    // Get list of potential parents: Business.Client -> Client
                    var coreClients = await _unitOfWork.BusinessClients.GetAllAsync();
                    foreach (var core in coreClients)
                        model.Clients.Add(new Models.Business.Client(core));


                    return View("/Views/Administration/Business/IdeaOtherRunningCost/Edit.cshtml", model);
                }


                // Process the forms content.
                await _unitOfWork.BusinessIdeaOtherRunningCosts.AddAsync(model.GetCore());


                if(await _unitOfWork.CompleteAsync() == DataStoreResult.Success)
                    return Redirect(
                        "/Administration/Business/IdeaOtherRunningCost");

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Edit.cshtml", model);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                ModelState.AddModelError(
                    "Error",
                    Models.Log.Message_CouldNotSave);

                return View("/Views/Administration/Business/IdeaOtherRunningCost/Edit.cshtml", model);
            }
        }

        [HttpPost("/Administration/Business/IdeaOtherRunningCost/Cancel")]
        public IActionResult Cancel()
        {
            return Redirect(
                "/Administration/Business/IdeaOtherRunningCost");
        }
    }
}