/*********************************************************
       Code Generated By  .  .  .  .  Delaney's ScriptBot
       WWW .  .  .  .  .  .  .  .  .  www.scriptbot.io
       Template Name.  .  .  .  .  .  Project Green 3.0
       Template Version.  .  .  .  .  20210407 014
       Author .  .  .  .  .  .  .  .  Delaney

                      ,        ,--,_
                       \ _ ___/ /\|
                       ( )__, )
                      |/_  '--,
                        \ `  / '

 *********************************************************/

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using System;
using System.Threading.Tasks;
using SilkFlo.Data.Core;

namespace SilkFlo.Web.Controllers.Administration.Shared
{
    public partial class IdeaAuthorisationController : Abstract
    {
        public IdeaAuthorisationController(IUnitOfWork unitOfWork,
                                  Services.ViewToString viewToString,
                                  IAuthorizationService authorisation) : base(unitOfWork, viewToString, authorisation) { }


        [HttpGet("/Administration/Shared/IdeaAuthorisation")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Index()
        {
            try
            {
                IEnumerable<Data.Core.Domain.Shared.IdeaAuthorisation> cores;

                cores = (await _unitOfWork.SharedIdeaAuthorisations
                                         .GetAllAsync())
                                         .ToArray();

                var models = new List<Models.Shared.IdeaAuthorisation>();

                if(cores != null)
                {
                    cores = cores.OrderBy(m => m.Sort)
                                 .ThenBy(m => m.Name);

                    foreach(var core in cores)
                    {
                        var model = new Models.Shared.IdeaAuthorisation(core);
                        models.Add(model);
                    }
                }

                var summary = new Models.Summary<Models.Shared.IdeaAuthorisation>
                {
                    Models = models,
                    Count = models.Count,
                };

                return View("/Views/Administration/Shared/IdeaAuthorisation/Index.cshtml", summary);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                var summary = new Models.Summary<Models.Shared.IdeaAuthorisation>
                {
                    Models = null,
                    Count = 0,
                };

                ModelState.AddModelError(
                        "Error",
                        Models.Log.Message_DatabaseErrorFetchList);

                return View("/Views/Administration/Shared/IdeaAuthorisation/Index.cshtml", summary);
            }
        }
        [HttpGet("/Administration/Shared/IdeaAuthorisation/Delete/{id}")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete (string id)
        {
            if (id == null)
                return Redirect("/Administration/shared/ideaAuthorisation");

            var ideaAuthorisation = await _unitOfWork.SharedIdeaAuthorisations.GetAsync(id);

            if (ideaAuthorisation == null)
                return Redirect("/Administration/Shared/IdeaAuthorisation");
            else
            {
                if (ideaAuthorisation.CreatedById != null)
                    ideaAuthorisation.CreatedBy = await _unitOfWork.Users.GetAsync(ideaAuthorisation.CreatedById);

                if (ideaAuthorisation.UpdatedById != null)
                    ideaAuthorisation.UpdatedBy = await _unitOfWork.Users.GetAsync(ideaAuthorisation.UpdatedById);

                var model = new Models.Shared.IdeaAuthorisation(ideaAuthorisation);

                return View("/Views/Administration/Shared/IdeaAuthorisation/delete.cshtml", model);
            }
        }
        [HttpPost("/Administration/Shared/IdeaAuthorisation/Delete")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete(Models.Shared.IdeaAuthorisation model)
        {
            if (model == null)
                return Redirect("/Administration/Shared/IdeaAuthorisation");


            var userId = GetUserId();

            if (userId == null)
            {
                ModelState.AddModelError("Error",
                                         "UserId is missing.");

                return View("/Views/Administration/Shared/IdeaAuthorisation/Delete.cshtml", model);
            }


            if (model.Id == null)
            {
                var message = "[POST] The primary key ID is missing.";

                ModelState.AddModelError("Error",
                                         message);

                _unitOfWork.Log(message,
                                Severity.Warning);

                await _unitOfWork.CompleteAsync();

                return View("/Views/Administration/Shared/IdeaAuthorisation/Delete.cshtml", model);
            }


            try
            {
                await _unitOfWork.SharedIdeaAuthorisations
                                 .RemoveAsync(model.Id);

                await _unitOfWork.CompleteAsync();

                return Redirect(
                    "/Administration/Shared/IdeaAuthorisation");
            }
            catch (ChildDependencyException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/IdeaAuthorisation/Delete.cshtml", model);
            }
            catch (NotFoundException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/IdeaAuthorisation/Delete.cshtml", model);
            }
            catch (Exception ex)
            {
                ModelState.AddModelError("Error",
                                         "Cannot delete the IdeaAuthorisation.");

                _unitOfWork.Log(ex);
                await _unitOfWork.CompleteAsync();
                return View("/Views/Administration/Shared/IdeaAuthorisation/Delete.cshtml", model);
            }
        }

        [HttpGet("/Administration/Shared/IdeaAuthorisation/Edit/{id}")]
        [HttpGet("/Administration/Shared/IdeaAuthorisation/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(string id)
        {
            Data.Core.Domain.Shared.IdeaAuthorisation ideaAuthorisation;

            if (string.IsNullOrWhiteSpace(id))
            {
                ideaAuthorisation = new Data.Core.Domain.Shared.IdeaAuthorisation
                {
                    Id = Guid.NewGuid().ToString(),
                };
            }
            else
            {
                ideaAuthorisation = await _unitOfWork.SharedIdeaAuthorisations.GetAsync(id);

                if (ideaAuthorisation == null)
                    return Redirect(
                        "/Administration/Shared/IdeaAuthorisation");
            }

            var model = new Models.Shared.IdeaAuthorisation(ideaAuthorisation);
            await model.GetCreatedAndUpdated(_unitOfWork);

            return View("/Views/Administration/Shared/IdeaAuthorisation/Edit.cshtml", model);
        }


        [HttpPost("/Administration/Shared/IdeaAuthorisation/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(Models.Shared.IdeaAuthorisation model)
        {
            try
            {
                // Get the userId
                var userId = GetUserId();


                // Is the userId present.
                // If not return to view.                
                if (userId == null)
                {
                    ModelState.AddModelError(
                            "Error",
                            "userId is missing.");

                    return View("/Views/Administration/Shared/IdeaAuthorisation/Edit.cshtml", model);
                }


                // Is the model valid?
                // If not return to view.
                bool isValid = Validate(_unitOfWork, model);

                var feedback = new ViewModels.Feedback();
                feedback = await model.CheckUniqueAsync(_unitOfWork, feedback);
                if (!feedback.IsValid)
                {
                    isValid = false;
                    foreach (var feedbackElement in feedback.Elements)
                        model.Errors.Add(feedbackElement.Value);
                }

                if (!isValid)
                {
                    return View("/Views/Administration/Shared/IdeaAuthorisation/Edit.cshtml", model);
                }


                // Process the forms content.
                await _unitOfWork.SharedIdeaAuthorisations.AddAsync(model.GetCore());


                if(await _unitOfWork.CompleteAsync() == DataStoreResult.Success)
                    return Redirect(
                        "/Administration/Shared/IdeaAuthorisation");

                return View("/Views/Administration/Shared/IdeaAuthorisation/Edit.cshtml", model);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                ModelState.AddModelError(
                    "Error",
                    Models.Log.Message_CouldNotSave);

                return View("/Views/Administration/Shared/IdeaAuthorisation/Edit.cshtml", model);
            }
        }

        [HttpPost("/Administration/Shared/IdeaAuthorisation/Cancel")]
        public IActionResult Cancel()
        {
            return Redirect(
                "/Administration/Shared/IdeaAuthorisation");
        }
    }
}