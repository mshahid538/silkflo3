/*********************************************************
       Code Generated By  .  .  .  .  Delaney's ScriptBot
       WWW .  .  .  .  .  .  .  .  .  www.scriptbot.io
       Template Name.  .  .  .  .  .  Project Green 3.0
       Template Version.  .  .  .  .  20210407 014
       Author .  .  .  .  .  .  .  .  Delaney

                      ,        ,--,_
                       \ _ ___/ /\|
                       ( )__, )
                      |/_  '--,
                        \ `  / '

 *********************************************************/

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using System;
using System.Threading.Tasks;
using SilkFlo.Data.Core;

namespace SilkFlo.Web.Controllers.Administration.Shared
{
    public partial class AverageNumberOfStepController : Abstract
    {
        public AverageNumberOfStepController(IUnitOfWork unitOfWork,
                                  Services.ViewToString viewToString,
                                  IAuthorizationService authorisation) : base(unitOfWork, viewToString, authorisation) { }


        [HttpGet("/Administration/Shared/AverageNumberOfStep")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Index()
        {
            try
            {
                IEnumerable<Data.Core.Domain.Shared.AverageNumberOfStep> cores;

                cores = (await _unitOfWork.SharedAverageNumberOfSteps
                                         .GetAllAsync())
                                         .ToArray();

                var models = new List<Models.Shared.AverageNumberOfStep>();

                if(cores != null)
                {
                    cores = cores.OrderByDescending(m => m.Weighting)
                                 .ThenBy(m => m.Name);

                    foreach(var core in cores)
                    {
                        var model = new Models.Shared.AverageNumberOfStep(core);
                        models.Add(model);
                    }
                }

                var summary = new Models.Summary<Models.Shared.AverageNumberOfStep>
                {
                    Models = models,
                    Count = models.Count,
                };

                return View("/Views/Administration/Shared/AverageNumberOfStep/Index.cshtml", summary);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                var summary = new Models.Summary<Models.Shared.AverageNumberOfStep>
                {
                    Models = null,
                    Count = 0,
                };

                ModelState.AddModelError(
                        "Error",
                        Models.Log.Message_DatabaseErrorFetchList);

                return View("/Views/Administration/Shared/AverageNumberOfStep/Index.cshtml", summary);
            }
        }
        [HttpGet("/Administration/Shared/AverageNumberOfStep/Delete/{id}")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete (string id)
        {
            if (id == null)
                return Redirect("/Administration/shared/averageNumberOfStep");

            var averageNumberOfStep = await _unitOfWork.SharedAverageNumberOfSteps.GetAsync(id);

            if (averageNumberOfStep == null)
                return Redirect("/Administration/Shared/AverageNumberOfStep");
            else
            {
                if (averageNumberOfStep.CreatedById != null)
                    averageNumberOfStep.CreatedBy = await _unitOfWork.Users.GetAsync(averageNumberOfStep.CreatedById);

                if (averageNumberOfStep.UpdatedById != null)
                    averageNumberOfStep.UpdatedBy = await _unitOfWork.Users.GetAsync(averageNumberOfStep.UpdatedById);

                var model = new Models.Shared.AverageNumberOfStep(averageNumberOfStep);

                return View("/Views/Administration/Shared/AverageNumberOfStep/delete.cshtml", model);
            }
        }
        [HttpPost("/Administration/Shared/AverageNumberOfStep/Delete")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete(Models.Shared.AverageNumberOfStep model)
        {
            if (model == null)
                return Redirect("/Administration/Shared/AverageNumberOfStep");


            var userId = GetUserId();

            if (userId == null)
            {
                ModelState.AddModelError("Error",
                                         "UserId is missing.");

                return View("/Views/Administration/Shared/AverageNumberOfStep/Delete.cshtml", model);
            }


            if (model.Id == null)
            {
                var message = "[POST] The primary key ID is missing.";

                ModelState.AddModelError("Error",
                                         message);

                _unitOfWork.Log(message,
                                Severity.Warning);

                await _unitOfWork.CompleteAsync();

                return View("/Views/Administration/Shared/AverageNumberOfStep/Delete.cshtml", model);
            }


            try
            {
                await _unitOfWork.SharedAverageNumberOfSteps
                                 .RemoveAsync(model.Id);

                await _unitOfWork.CompleteAsync();

                return Redirect(
                    "/Administration/Shared/AverageNumberOfStep");
            }
            catch (ChildDependencyException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/AverageNumberOfStep/Delete.cshtml", model);
            }
            catch (NotFoundException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/AverageNumberOfStep/Delete.cshtml", model);
            }
            catch (Exception ex)
            {
                ModelState.AddModelError("Error",
                                         "Cannot delete the AverageNumberOfStep.");

                _unitOfWork.Log(ex);
                await _unitOfWork.CompleteAsync();
                return View("/Views/Administration/Shared/AverageNumberOfStep/Delete.cshtml", model);
            }
        }

        [HttpGet("/Administration/Shared/AverageNumberOfStep/Edit/{id}")]
        [HttpGet("/Administration/Shared/AverageNumberOfStep/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(string id)
        {
            Data.Core.Domain.Shared.AverageNumberOfStep averageNumberOfStep;

            if (string.IsNullOrWhiteSpace(id))
            {
                averageNumberOfStep = new Data.Core.Domain.Shared.AverageNumberOfStep
                {
                    Id = Guid.NewGuid().ToString(),
                };
            }
            else
            {
                averageNumberOfStep = await _unitOfWork.SharedAverageNumberOfSteps.GetAsync(id);

                if (averageNumberOfStep == null)
                    return Redirect(
                        "/Administration/Shared/AverageNumberOfStep");
            }

            var model = new Models.Shared.AverageNumberOfStep(averageNumberOfStep);
            await model.GetCreatedAndUpdated(_unitOfWork);

            return View("/Views/Administration/Shared/AverageNumberOfStep/Edit.cshtml", model);
        }


        [HttpPost("/Administration/Shared/AverageNumberOfStep/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(Models.Shared.AverageNumberOfStep model)
        {
            try
            {
                // Get the userId
                var userId = GetUserId();


                // Is the userId present.
                // If not return to view.                
                if (userId == null)
                {
                    ModelState.AddModelError(
                            "Error",
                            "userId is missing.");

                    return View("/Views/Administration/Shared/AverageNumberOfStep/Edit.cshtml", model);
                }


                // Is the model valid?
                // If not return to view.
                bool isValid = Validate(_unitOfWork, model);

                var feedback = new ViewModels.Feedback();
                feedback = await model.CheckUniqueAsync(_unitOfWork, feedback);
                if (!feedback.IsValid)
                {
                    isValid = false;
                    foreach (var feedbackElement in feedback.Elements)
                        model.Errors.Add(feedbackElement.Value);
                }

                if (!isValid)
                {
                    return View("/Views/Administration/Shared/AverageNumberOfStep/Edit.cshtml", model);
                }


                // Process the forms content.
                await _unitOfWork.SharedAverageNumberOfSteps.AddAsync(model.GetCore());


                if(await _unitOfWork.CompleteAsync() == DataStoreResult.Success)
                    return Redirect(
                        "/Administration/Shared/AverageNumberOfStep");

                return View("/Views/Administration/Shared/AverageNumberOfStep/Edit.cshtml", model);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                ModelState.AddModelError(
                    "Error",
                    Models.Log.Message_CouldNotSave);

                return View("/Views/Administration/Shared/AverageNumberOfStep/Edit.cshtml", model);
            }
        }

        [HttpPost("/Administration/Shared/AverageNumberOfStep/Cancel")]
        public IActionResult Cancel()
        {
            return Redirect(
                "/Administration/Shared/AverageNumberOfStep");
        }
    }
}