/*********************************************************
       Code Generated By  .  .  .  .  Delaney's ScriptBot
       WWW .  .  .  .  .  .  .  .  .  www.scriptbot.io
       Template Name.  .  .  .  .  .  Project Green 3.0
       Template Version.  .  .  .  .  20210407 014
       Author .  .  .  .  .  .  .  .  Delaney

                      ,        ,--,_
                       \ _ ___/ /\|
                       ( )__, )
                      |/_  '--,
                        \ `  / '

 *********************************************************/

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using System;
using System.Threading.Tasks;
using SilkFlo.Data.Core;

namespace SilkFlo.Web.Controllers.Administration.Shared
{
    public partial class DataInputPercentOfStructuredController : Abstract
    {
        public DataInputPercentOfStructuredController(IUnitOfWork unitOfWork,
                                  Services.ViewToString viewToString,
                                  IAuthorizationService authorisation) : base(unitOfWork, viewToString, authorisation) { }


        [HttpGet("/Administration/Shared/DataInputPercentOfStructured")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Index()
        {
            try
            {
                IEnumerable<Data.Core.Domain.Shared.DataInputPercentOfStructured> cores;

                cores = (await _unitOfWork.SharedDataInputPercentOfStructureds
                                         .GetAllAsync())
                                         .ToArray();

                var models = new List<Models.Shared.DataInputPercentOfStructured>();

                if(cores != null)
                {
                    cores = cores.OrderByDescending(m => m.Weighting)
                                 .ThenBy(m => m.Name);

                    foreach(var core in cores)
                    {
                        var model = new Models.Shared.DataInputPercentOfStructured(core);
                        models.Add(model);
                    }
                }

                var summary = new Models.Summary<Models.Shared.DataInputPercentOfStructured>
                {
                    Models = models,
                    Count = models.Count,
                };

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Index.cshtml", summary);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                var summary = new Models.Summary<Models.Shared.DataInputPercentOfStructured>
                {
                    Models = null,
                    Count = 0,
                };

                ModelState.AddModelError(
                        "Error",
                        Models.Log.Message_DatabaseErrorFetchList);

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Index.cshtml", summary);
            }
        }
        [HttpGet("/Administration/Shared/DataInputPercentOfStructured/Delete/{id}")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete (string id)
        {
            if (id == null)
                return Redirect("/Administration/shared/dataInputPercentOfStructured");

            var dataInputPercentOfStructured = await _unitOfWork.SharedDataInputPercentOfStructureds.GetAsync(id);

            if (dataInputPercentOfStructured == null)
                return Redirect("/Administration/Shared/DataInputPercentOfStructured");
            else
            {
                if (dataInputPercentOfStructured.CreatedById != null)
                    dataInputPercentOfStructured.CreatedBy = await _unitOfWork.Users.GetAsync(dataInputPercentOfStructured.CreatedById);

                if (dataInputPercentOfStructured.UpdatedById != null)
                    dataInputPercentOfStructured.UpdatedBy = await _unitOfWork.Users.GetAsync(dataInputPercentOfStructured.UpdatedById);

                var model = new Models.Shared.DataInputPercentOfStructured(dataInputPercentOfStructured);

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/delete.cshtml", model);
            }
        }
        [HttpPost("/Administration/Shared/DataInputPercentOfStructured/Delete")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete(Models.Shared.DataInputPercentOfStructured model)
        {
            if (model == null)
                return Redirect("/Administration/Shared/DataInputPercentOfStructured");


            var userId = GetUserId();

            if (userId == null)
            {
                ModelState.AddModelError("Error",
                                         "UserId is missing.");

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Delete.cshtml", model);
            }


            if (model.Id == null)
            {
                var message = "[POST] The primary key ID is missing.";

                ModelState.AddModelError("Error",
                                         message);

                _unitOfWork.Log(message,
                                Severity.Warning);

                await _unitOfWork.CompleteAsync();

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Delete.cshtml", model);
            }


            try
            {
                await _unitOfWork.SharedDataInputPercentOfStructureds
                                 .RemoveAsync(model.Id);

                await _unitOfWork.CompleteAsync();

                return Redirect(
                    "/Administration/Shared/DataInputPercentOfStructured");
            }
            catch (ChildDependencyException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Delete.cshtml", model);
            }
            catch (NotFoundException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Delete.cshtml", model);
            }
            catch (Exception ex)
            {
                ModelState.AddModelError("Error",
                                         "Cannot delete the DataInputPercentOfStructured.");

                _unitOfWork.Log(ex);
                await _unitOfWork.CompleteAsync();
                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Delete.cshtml", model);
            }
        }

        [HttpGet("/Administration/Shared/DataInputPercentOfStructured/Edit/{id}")]
        [HttpGet("/Administration/Shared/DataInputPercentOfStructured/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(string id)
        {
            Data.Core.Domain.Shared.DataInputPercentOfStructured dataInputPercentOfStructured;

            if (string.IsNullOrWhiteSpace(id))
            {
                dataInputPercentOfStructured = new Data.Core.Domain.Shared.DataInputPercentOfStructured
                {
                    Id = Guid.NewGuid().ToString(),
                };
            }
            else
            {
                dataInputPercentOfStructured = await _unitOfWork.SharedDataInputPercentOfStructureds.GetAsync(id);

                if (dataInputPercentOfStructured == null)
                    return Redirect(
                        "/Administration/Shared/DataInputPercentOfStructured");
            }

            var model = new Models.Shared.DataInputPercentOfStructured(dataInputPercentOfStructured);
            await model.GetCreatedAndUpdated(_unitOfWork);

            return View("/Views/Administration/Shared/DataInputPercentOfStructured/Edit.cshtml", model);
        }


        [HttpPost("/Administration/Shared/DataInputPercentOfStructured/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(Models.Shared.DataInputPercentOfStructured model)
        {
            try
            {
                // Get the userId
                var userId = GetUserId();


                // Is the userId present.
                // If not return to view.                
                if (userId == null)
                {
                    ModelState.AddModelError(
                            "Error",
                            "userId is missing.");

                    return View("/Views/Administration/Shared/DataInputPercentOfStructured/Edit.cshtml", model);
                }


                // Is the model valid?
                // If not return to view.
                bool isValid = Validate(_unitOfWork, model);

                var feedback = new ViewModels.Feedback();
                feedback = await model.CheckUniqueAsync(_unitOfWork, feedback);
                if (!feedback.IsValid)
                {
                    isValid = false;
                    foreach (var feedbackElement in feedback.Elements)
                        model.Errors.Add(feedbackElement.Value);
                }

                if (!isValid)
                {
                    return View("/Views/Administration/Shared/DataInputPercentOfStructured/Edit.cshtml", model);
                }


                // Process the forms content.
                await _unitOfWork.SharedDataInputPercentOfStructureds.AddAsync(model.GetCore());


                if(await _unitOfWork.CompleteAsync() == DataStoreResult.Success)
                    return Redirect(
                        "/Administration/Shared/DataInputPercentOfStructured");

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Edit.cshtml", model);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                ModelState.AddModelError(
                    "Error",
                    Models.Log.Message_CouldNotSave);

                return View("/Views/Administration/Shared/DataInputPercentOfStructured/Edit.cshtml", model);
            }
        }

        [HttpPost("/Administration/Shared/DataInputPercentOfStructured/Cancel")]
        public IActionResult Cancel()
        {
            return Redirect(
                "/Administration/Shared/DataInputPercentOfStructured");
        }
    }
}