/*********************************************************
       Code Generated By  .  .  .  .  Delaney's ScriptBot
       WWW .  .  .  .  .  .  .  .  .  www.scriptbot.io
       Template Name.  .  .  .  .  .  Project Green 3.0
       Template Version.  .  .  .  .  20210407 014
       Author .  .  .  .  .  .  .  .  Delaney

                      ,        ,--,_
                       \ _ ___/ /\|
                       ( )__, )
                      |/_  '--,
                        \ `  / '

 *********************************************************/

using Microsoft.AspNetCore.Authorization;
using Microsoft.AspNetCore.Mvc;
using System.Collections.Generic;
using System.Linq;
using System;
using System.Threading.Tasks;
using SilkFlo.Data.Core;

namespace SilkFlo.Web.Controllers.Administration.Shared
{
    public partial class ProcessPeakController : Abstract
    {
        public ProcessPeakController(IUnitOfWork unitOfWork,
                                  Services.ViewToString viewToString,
                                  IAuthorizationService authorisation) : base(unitOfWork, viewToString, authorisation) { }


        [HttpGet("/Administration/Shared/ProcessPeak")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Index()
        {
            try
            {
                IEnumerable<Data.Core.Domain.Shared.ProcessPeak> cores;

                cores = (await _unitOfWork.SharedProcessPeaks
                                         .GetAllAsync())
                                         .ToArray();

                var models = new List<Models.Shared.ProcessPeak>();

                if(cores != null)
                {
                    cores = cores.OrderByDescending(m => m.Weighting)
                                 .ThenBy(m => m.Name);

                    foreach(var core in cores)
                    {
                        var model = new Models.Shared.ProcessPeak(core);
                        models.Add(model);
                    }
                }

                var summary = new Models.Summary<Models.Shared.ProcessPeak>
                {
                    Models = models,
                    Count = models.Count,
                };

                return View("/Views/Administration/Shared/ProcessPeak/Index.cshtml", summary);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                var summary = new Models.Summary<Models.Shared.ProcessPeak>
                {
                    Models = null,
                    Count = 0,
                };

                ModelState.AddModelError(
                        "Error",
                        Models.Log.Message_DatabaseErrorFetchList);

                return View("/Views/Administration/Shared/ProcessPeak/Index.cshtml", summary);
            }
        }
        [HttpGet("/Administration/Shared/ProcessPeak/Delete/{id}")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete (string id)
        {
            if (id == null)
                return Redirect("/Administration/shared/processPeak");

            var processPeak = await _unitOfWork.SharedProcessPeaks.GetAsync(id);

            if (processPeak == null)
                return Redirect("/Administration/Shared/ProcessPeak");
            else
            {
                if (processPeak.CreatedById != null)
                    processPeak.CreatedBy = await _unitOfWork.Users.GetAsync(processPeak.CreatedById);

                if (processPeak.UpdatedById != null)
                    processPeak.UpdatedBy = await _unitOfWork.Users.GetAsync(processPeak.UpdatedById);

                var model = new Models.Shared.ProcessPeak(processPeak);

                return View("/Views/Administration/Shared/ProcessPeak/delete.cshtml", model);
            }
        }
        [HttpPost("/Administration/Shared/ProcessPeak/Delete")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Delete(Models.Shared.ProcessPeak model)
        {
            if (model == null)
                return Redirect("/Administration/Shared/ProcessPeak");


            var userId = GetUserId();

            if (userId == null)
            {
                ModelState.AddModelError("Error",
                                         "UserId is missing.");

                return View("/Views/Administration/Shared/ProcessPeak/Delete.cshtml", model);
            }


            if (model.Id == null)
            {
                var message = "[POST] The primary key ID is missing.";

                ModelState.AddModelError("Error",
                                         message);

                _unitOfWork.Log(message,
                                Severity.Warning);

                await _unitOfWork.CompleteAsync();

                return View("/Views/Administration/Shared/ProcessPeak/Delete.cshtml", model);
            }


            try
            {
                await _unitOfWork.SharedProcessPeaks
                                 .RemoveAsync(model.Id);

                await _unitOfWork.CompleteAsync();

                return Redirect(
                    "/Administration/Shared/ProcessPeak");
            }
            catch (ChildDependencyException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/ProcessPeak/Delete.cshtml", model);
            }
            catch (NotFoundException ex)
            {
                ModelState.AddModelError("Error",
                                         ex.Message);
                return View("/Views/Administration/Shared/ProcessPeak/Delete.cshtml", model);
            }
            catch (Exception ex)
            {
                ModelState.AddModelError("Error",
                                         "Cannot delete the ProcessPeak.");

                _unitOfWork.Log(ex);
                await _unitOfWork.CompleteAsync();
                return View("/Views/Administration/Shared/ProcessPeak/Delete.cshtml", model);
            }
        }

        [HttpGet("/Administration/Shared/ProcessPeak/Edit/{id}")]
        [HttpGet("/Administration/Shared/ProcessPeak/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(string id)
        {
            Data.Core.Domain.Shared.ProcessPeak processPeak;

            if (string.IsNullOrWhiteSpace(id))
            {
                processPeak = new Data.Core.Domain.Shared.ProcessPeak
                {
                    Id = Guid.NewGuid().ToString(),
                };
            }
            else
            {
                processPeak = await _unitOfWork.SharedProcessPeaks.GetAsync(id);

                if (processPeak == null)
                    return Redirect(
                        "/Administration/Shared/ProcessPeak");
            }

            var model = new Models.Shared.ProcessPeak(processPeak);
            await model.GetCreatedAndUpdated(_unitOfWork);

            return View("/Views/Administration/Shared/ProcessPeak/Edit.cshtml", model);
        }


        [HttpPost("/Administration/Shared/ProcessPeak/Edit")]
        [Authorize(Policy = Policy.Administrator)]
        public async Task<IActionResult> Edit(Models.Shared.ProcessPeak model)
        {
            try
            {
                // Get the userId
                var userId = GetUserId();


                // Is the userId present.
                // If not return to view.                
                if (userId == null)
                {
                    ModelState.AddModelError(
                            "Error",
                            "userId is missing.");

                    return View("/Views/Administration/Shared/ProcessPeak/Edit.cshtml", model);
                }


                // Is the model valid?
                // If not return to view.
                bool isValid = Validate(_unitOfWork, model);

                var feedback = new ViewModels.Feedback();
                feedback = await model.CheckUniqueAsync(_unitOfWork, feedback);
                if (!feedback.IsValid)
                {
                    isValid = false;
                    foreach (var feedbackElement in feedback.Elements)
                        model.Errors.Add(feedbackElement.Value);
                }

                if (!isValid)
                {
                    return View("/Views/Administration/Shared/ProcessPeak/Edit.cshtml", model);
                }


                // Process the forms content.
                await _unitOfWork.SharedProcessPeaks.AddAsync(model.GetCore());


                if(await _unitOfWork.CompleteAsync() == DataStoreResult.Success)
                    return Redirect(
                        "/Administration/Shared/ProcessPeak");

                return View("/Views/Administration/Shared/ProcessPeak/Edit.cshtml", model);
            }
            catch (Exception ex)
            {
                _unitOfWork.Log(ex);

                ModelState.AddModelError(
                    "Error",
                    Models.Log.Message_CouldNotSave);

                return View("/Views/Administration/Shared/ProcessPeak/Edit.cshtml", model);
            }
        }

        [HttpPost("/Administration/Shared/ProcessPeak/Cancel")]
        public IActionResult Cancel()
        {
            return Redirect(
                "/Administration/Shared/ProcessPeak");
        }
    }
}